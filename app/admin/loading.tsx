/**
 * Admin Loading Component
 * 
 * Removed as per user request - no loading screen needed
 * Pages now load directly with skeleton loaders in components
 */

export default function AdminLoading() {
  return null;
}
